# Discord Bot Configuration
# Get these from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here
DISCORD_CLIENT_ID=your_discord_application_client_id_here

# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database

# Environment
# Set to 'production' when deploying to Railway
NODE_ENV=development

# GitHub Backup Configuration (Required for automatic backups)
# These are required for the CSV backup system to push to GitHub
GITHUB_USERNAME=your_github_username
GITHUB_REPO=your_username/n8n_discord_bot
GITHUB_TOKEN=your_github_personal_access_token

# GitHub Token Setup Instructions:
# 1. Go to GitHub.com → Settings → Developer settings → Personal access tokens → Tokens (classic)
# 2. Generate new token with 'repo' scope (full control of private repositories)
# 3. Copy the token and set it as GITHUB_TOKEN environment variable
# 4. Make sure GITHUB_REPO matches your repository name (e.g., 'username/repo-name')

# Optional: Custom backup settings
# BACKUP_INTERVAL=0 * * * *  # Cron expression for backup frequency (default: hourly)
# BACKUP_RETENTION=24        # Number of backups to keep (default: 24) 